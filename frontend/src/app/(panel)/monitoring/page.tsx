import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function MonitoringPage() {
  return (
    <PlaceholderPage
      title="Monitoring"
      description="Live logs, process monitor, service status, and cronjob manager."
    />
  );
}

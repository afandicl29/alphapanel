import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function TerminalPage() {
  return (
    <PlaceholderPage
      title="Web SSH Terminal"
      description="xterm.js terminal connected via node-pty through AlphaPanel daemon."
    />
  );
}

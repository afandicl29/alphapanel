import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function DockerPage() {
  return (
    <PlaceholderPage
      title="Docker Integration"
      description="Create containers, restart, and Docker Compose deployments."
    />
  );
}

import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function SslPage() {
  return (
    <PlaceholderPage
      title="SSL Management"
      description="Let's Encrypt auto-renew, manual SSL upload, and certificate management."
    />
  );
}
